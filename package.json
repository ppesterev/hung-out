{
  "name": "hung-out",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"ts-node-dev --transpile-only src/server/server.ts\"",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "start": "node dist/server/server.js"
  },
  "dependencies": {
    "mime-types": "^2.1.33",
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "@types/mime-types": "^2.1.1",
    "@types/node": "^16.11.6",
    "concurrently": "^6.3.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
